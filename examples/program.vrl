# This is a comment

"Hello world"

s'Hello'

obj = {
  "boolean": true,
  "string": s'Here is a string',
  "object": {
    "boolean": true,
    "string": s"here is a nested string"
  }
}

fruits = ["apple", "orange", "banana", [1, 2, 3], null, 37.5]

my_regex = r'[+-]?([0-9]*[.])?[0-9]+'
my_other_regex = r"[+-]?([0-9]*[.])?[0-9]+"